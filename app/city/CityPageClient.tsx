/**
 * CityPageClient.tsx — DEPRECATED.
 * Этот компонент не используется.
 * Весь контент каталога городов рендерится серверно в app/city/page.tsx.
 * Файл сохранён для истории — безопасно удалить.
 */
export default function CityPageClient() {
  return null;
}
